# Prompt / outcome — Candlestick Bar

## Prompt

Continue the complete Pending indicator-library overhaul: inspect every
remaining DeepCharts study and licensed DLL evidence, implement real logic and
settings, and do not remove Pending cosmetically.

## Fixed

- Implemented the independent Candlestick Bar overlay with Minutes, Range and
  DeepCharts-style target/reversal Vol Bars.
- Corrected the shared event-bar router, which previously sent Vol Bars
  through Range logic and ignored Parameter 2.
- Added exact one-print allocation, `flowOnly` rejection, theme/custom colors,
  fill, width, border, opacity, close boundaries, persistence and settings UI.
- Added focused construction, data-integrity, catalogue and UI wiring tests.

## Outcome

Candlestick Bar is genuinely addable. The authoritative catalogue is now 127
total, 118 registered and 9 pending. Event modes deliberately show no invented
history when exact executions are unavailable.
