# Prompt / outcome — Annotations Overlay

## Prompt

Continue engineering every Pending DeepCharts-referenced indicator with real
logic, settings, data, visuals, theme behavior, persistence and performance.

## Fixed

- Added a workspace-local chart annotation registry keyed by Chart ID and
  Indicator ID, matching the public DeepCharts workflow.
- Added live source publishing, target subscription, target-owned copies,
  event-chart time alignment, theme/source colour control and persistence.
- Added source cleanup and self-reference guards so stale or recursive copies
  are not rendered.

## Outcome

Annotations Overlay is genuinely addable. The catalogue is now 127 total, 121
registered and 6 pending. Cross-process mirroring and non-indicator/manual
drawing import are not claimed.
