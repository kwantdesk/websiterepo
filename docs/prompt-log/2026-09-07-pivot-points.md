# Prompt — complete every Pending indicator using DeepCharts evidence

## Request

Continue the earlier pending-indicator overhaul, use the licensed DeepCharts
DLL/reference, engineer every real study and do not leave catalogue placeholders.

## Fix

Selected Pivot Points as the next bounded study because its public calculation
and installed-DLL settings contract can both be verified. Added prior-period
P/R1/R2/S1/S2 calculation, daily and Minute/Hour/Day/Week references,
exchange-time custom sessions, bounded period history, exact settings controls,
theme/custom colours and a chart-attached label renderer.

## Outcome

Pivot Points moves from Pending to Add only after seven focused tests, TypeScript,
lint, a complete production build, shared settings/template/theme checks and
synthetic browser rendering/settings QA. Its evidence and limitations are recorded in
`docs/deepcharts-pivot-points-audit.md`. The wider library now has 27 Pending
entries; Auction Gap remains gated for market-open soak even though its new VPS
continuity protocol is deployed and verified in the closed session.
